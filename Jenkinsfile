pipeline {
    agent any
    stages {
        stage('Build') {
            steps {RockCorn20

                sh "echo build"
                sh "./gradlew build"
            }
        }
        
    }
}