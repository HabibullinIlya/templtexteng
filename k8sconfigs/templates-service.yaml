apiVersion: v1
kind: Service
metadata:
  name: templates-microservice
  labels:
    app: templates-microservice
spec:
  type: NodePort
  ports:
   - port: 8080
     nodePort: 30081
  selector:
   app: templates-microservice